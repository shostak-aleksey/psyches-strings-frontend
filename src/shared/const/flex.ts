export enum Justify {
  start = 'start',
  center = 'center',
  end = 'end',
  between = 'between',
  around = 'around',
}

export enum Align {
  start = 'start',
  center = 'center',
  end = 'end',
}

export enum Direction {
  row = 'row',
  column = 'column',
  rowReverse = 'rowReverse',
  columnReverse = 'columnReverse',
}

export enum Gap {
  zero = 0,
  four = 4,
  eight = 8,
  twelve = 12,
  sixteen = 16,
  twenty = 20,
  twentyFour = 24,
  twentyEight = 28,
  thirtyTwo = 32,
}
